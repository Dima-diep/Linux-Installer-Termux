# Linux-Installer-Termux
Installer any Linux on Termux

Old version can't work at very much people. Now, I've fix it

How install:

$ apt install toilet figlet ruby2(or ruby)
$ gem install lolcat
$ pip install -r requirements.txt
$ python linux.py

or, you can do it:

$ python linux2.py
